# filter () = creates a collection of elements from an iterable ** for which a function returns true **
# # filter (function, iterable)

friends = [("Rachel", 19),
           ("Monica", 18), ("Phoebe", 17),
           ("Joey", 16),
           ("Chandler", 21),
           ("Ross", 20)]

# get_age = lambda x: x[1]>18


def get_age(x): return x[1] > 18


new_frinds = list(filter(get_age, friends))

for i in new_frinds:
    print(i)
